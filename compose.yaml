services:
  spacesynth-warrior:
    image: ${IMAGE_NAME:-spacesynth-warrior:latest}
    container_name: spacesynth-warrior
    restart: unless-stopped
    ports:
      - "${PORT:-8080}:8080"
    environment:
      PORT: "8080"
